setwd("~/software/bsvs/R")
?crossprod
x = crossprod(matrix(rnorm(1000^2),1000))
R = chol(x)
R
dim(x)
dim(R)
y = rnorm(1000)
y
library(microbenchmark)
microbenchmark(a = R %*% y,b = backsolve(R,y,transpose = T),times = 10)
microbenchmark(a = R %*% y,b = backsolve(R,y,transpose = T),times = 100)
library(Matrix)
R = as(R,"dtCMatrix")
class(R)
R@uplo
microbenchmark(a = R %*% y,b = backsolve(R,y,transpose = T),times = 100)
microbenchmark(a = R %*% y,b = backsolve(R,y,transpose = T),times = 100)
x = crossprod(matrix(rnorm(5000^2),5000))
R = chol(x)
y = rnorm(nrow(x))
microbenchmark(a = R %*% y,b = backsolve(R,y,transpose = T),times = 10)
microbenchmark(a = R %*% y,b = backsolve(R,y,transpose = T),times = 100)
dim(R)
microbenchmark(a = R %*% y,b = backsolve(R,y,transpose = T),times = 500)
microbenchmark(a={z <- R %*% y},b = {u <- backsolve(R,y,transpose = T)},times = 500)
microbenchmark(a={z <- R %*% y},b = {u <- backsolve(R,y,transpose = T)},times = 50)
microbenchmark(qq={z <- R %*% y},b = {mm <- backsolve(R,y,transpose = T)},times = 50)
um=Reduce
?Reduce
getwd()
